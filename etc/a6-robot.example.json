{
  "_README": [
    "REAL-A6 viam-server ROBOT config for #37 (module-on-real-A6). Wraps the",
    "etc/a6-hardware.example.json component in a full machine config (components +",
    "modules). EXAMPLE-SAFE: set the two host-specific values marked <SET-ME> before use.",
    "  1) modules[0].executable_path -> the unpacked module entrypoint (after `viam module` build/unpack).",
    "  2) components[0].attributes.interface -> the real EtherCAT NIC (e.g. enp86s0).",
    "See docs/a6-bringup-runbook.md + docs/deployment-capabilities.md (caps).",
    "!!! Confirm counts_per_rev vs THIS drive's ESI/nameplate before energizing !!!"
  ],
  "modules": [
    {
      "type": "local",
      "name": "ethercat-servo",
      "executable_path": "<SET-ME: /abs/path/to/unpacked/module/entrypoint>"
    }
  ],
  "components": [
    {
      "_README": [
        "REAL-HARDWARE A6-EC servo config for the viam:ethercat:a6-servo motor model.",
        "!!! CONFIRM the encoder/gear/current values vs THIS drive's ESI + the motor",
        "nameplate before applying power. require_realtime is true -- the host needs",
        "CAP_SYS_NICE / RLIMIT_RTPRIO or start() throws.",
        "#18: NO control_mode and NO rxpdo/txpdo -- the driver is ALWAYS switch-capable",
        "(GoTo/GoFor -> PP, SetRPM -> PV at runtime) and defines the fixed CiA402",
        "superset PDO map itself (standard object indices)."
      ],
      "name": "servo",
      "api": "rdk:component:motor",
      "model": "viam:ethercat:a6-servo",
      "attributes": {
        "_comment_model": "#15 item 2: the A6 uses the viam:ethercat:a6-servo model -- an A6ServoDriver subclass carrying the vendor fault-reset (0x2031:01=1), the no-SYNC0 code (0x8700/Er74.1), and its 0x603F gloss in CODE, not config.",
        "_comment_bus": "interface = the host NIC bound to the EtherCAT segment (CONFIRM: `ip link`). slave = 1-based ring position of the A6.",
        "interface": "<SET-ME: enp86s0>",
        "slave": 1,
        "_comment_motor": "CONFIRM against the MOTOR nameplate (these are motor-, not drive-, specific): rated speed, rated current, peak current.",
        "max_rpm": 3000.0,
        "motor_rated_current_amps": 2.5,
        "_comment_kinematics": "counts_per_rev = 2^17 = 131072 (A6 single-turn encoder resolution -- CONFIRM the encoder bits on the nameplate/ESI). gear_ratio = motor revs per output rev (CONFIRM the mechanical reduction; 1.0 = direct drive).",
        "counts_per_rev": 131072.0,
        "gear_ratio": 1.0,
        "_comment_rt": "require_realtime=true: hard-fail if SCHED_FIFO is unavailable. loop_rate_hz <= 1000. use_distributed_clocks=true is REQUIRED for the A6 (DC-only sync; without SYNC0 it faults out of OP -- Er74.1). The SYNC0 cycle = 1e9/loop_rate_hz must be an integer multiple of 250us -> loop_rate_hz in {1000,500,250,...}. sync_cycle_granularity_ns=250000 rejects a bad rate at CONFIG time instead of at OP entry (Er74.0).",
        "require_realtime": true,
        "rt_priority": 80,
        "loop_rate_hz": 1000,
        "use_distributed_clocks": true,
        "sync_cycle_granularity_ns": 250000,
        "_comment_tuning": "Move-complete tuning -- CONFIRM on the bench. position_tolerance_counts is the position-stability band (counts); reached/is_moving is position-stability based (#59), not velocity-gated.",
        "position_tolerance_counts": 200,
        "command_queue_capacity": 64,
        "max_consecutive_wkc_errors": 5
      }
    }
  ]
}
